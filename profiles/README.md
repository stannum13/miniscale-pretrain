# Profiles

Store Nsight Systems or `torch.profiler` traces here. Start with the built-in phase and DDP communication counters; capture a trace only for the smallest run that reproduces a diagnosed gap. Record the exact command, GPU model, driver, CUDA/PyTorch versions, and trace filename in `SCALE_STATE.md`.
